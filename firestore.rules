rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for authenticated users on all collections
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}